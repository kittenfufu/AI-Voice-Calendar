# PRD — 语音日历 (Voice Calendar)

## 1. 产品目标

打造一个以语音为主要交互方式的日历/提醒事项工具，用户无需打字，对着麦克风说话即可完成日程的添加、查询、修改和删除。

## 2. 参考产品分析：Apple 提醒事项

Apple 提醒事项的核心设计逻辑（我们将其作为 UI 和数据结构基础）：

### 2.1 信息架构

```
提醒事项
├── 我的列表
│   ├── 今天          ← 当天到期的事项
│   ├── 计划中        ← 有截止日期的未来事项
│   ├── 全部          ← 所有事项
│   └── 已完成        ← 已勾选完成的事项
└── 自定义列表（工作、个人、购物等）
```

### 2.2 事件数据结构

每个提醒事项包含：
- **标题**（必填）：事件描述，如"开周会"
- **截止日期**（可选）：年月日
- **截止时间**（可选）：具体时间点
- **优先级**（可选）：高 / 中 / 低 / 无
- **备注**（可选）：附加说明
- **提醒方式**：在截止时间提醒 / 提前 N 分钟提醒
- **完成状态**：未完成 / 已完成

### 2.3 Apple 提醒事项没有的——我们要加的

- 🎤 **全程语音交互**：添加、查询、修改、删除均可语音完成
- 🔊 **语音播报确认**：操作完成后 AI 朗读确认内容
- 🧠 **自然语言理解**：理解"后天""下周一""明天下午三点半"等模糊表达

---

## 3. 用户故事

### 添加事件
> "我要在明天下午三点开产品评审会，提前30分钟提醒我"

系统行为：
1. 识别语音文字
2. 调用 Claude API 解析：`{ title: "产品评审会", date: "明天", time: "15:00", remindBefore: 30 }`
3. 存储事件
4. 语音播报："已添加：明天下午三点 产品评审会，将在两点半提醒你"

### 查询事件
> "今天有什么安排？"

系统行为：
1. 解析意图为"查询今天事件"
2. 读取当天所有事件
3. 语音播报列表

### 修改事件
> "把产品评审会改到四点"

系统行为：
1. 模糊匹配最近同名事件
2. 修改时间字段
3. 语音确认

### 删除事件
> "取消明天的评审会"

系统行为：
1. 匹配事件
2. 二次确认（语音："确认删除产品评审会吗？"）
3. 删除后语音播报确认

---

## 4. 功能优先级（MVP）

### P0 — 必须有
- [x] 语音录入（Web Speech API）
- [x] AI 意图解析（Claude API）
- [x] 事件添加 / 查询
- [x] 列表 UI（今天 / 全部）
- [x] 本地存储

### P1 — 应该有
- [x] 事件修改 / 删除
- [x] 语音播报反馈
- [x] 浏览器提醒通知
- [x] 优先级标签

### P2 — 加分项
- [ ] 桌面软件打包（Electron）
- [ ] 自定义列表
- [ ] 重复事件（每周一 9 点）
- [ ] 日历视图（月历）

---

## 5. 技术方案

### 语音识别
使用 **Web Speech API**（`SpeechRecognition`）：
- 免费，无需 API Key
- 支持中文（lang: 'zh-CN'）
- 局限：需要 Chrome 或 Edge 浏览器；无法在 Safari 使用（可提示用户）

备选：**OpenAI Whisper API**（付费，准确率更高，支持所有浏览器）

### AI 意图解析
调用 **Claude API**（claude-sonnet-4-20250514），传入：
```
系统提示：你是一个日历助手，将用户的语音文字转为 JSON 格式的日历操作指令...
用户输入：{识别出的文字}
当前时间：{当前时间戳，用于解析"明天""下周"等相对时间}
```

返回结构化 JSON：
```json
{
  "action": "add" | "query" | "update" | "delete",
  "event": {
    "title": "产品评审会",
    "date": "2026-05-31",
    "time": "15:00",
    "remindBeforeMinutes": 30,
    "priority": "high" | "medium" | "low" | "none"
  },
  "reply": "已添加：明天下午三点产品评审会"
}
```

### 语音播报
使用 **Web Speech Synthesis API**：
```javascript
const utterance = new SpeechSynthesisUtterance(replyText);
utterance.lang = 'zh-CN';
window.speechSynthesis.speak(utterance);
```

---

## 6. UI 设计规范（参考 Apple 提醒事项）

- 背景：纯白 / 浅灰交替
- 字体：系统字体，中文优先
- 事件卡片：左侧圆形勾选框，右侧标题 + 时间标签
- 优先级：红色感叹号（高）/ 橙色（中）/ 蓝色（低）
- 语音按钮：大圆形麦克风图标，录音时显示波形动画
- 分组标题：今天、明天、本周、更晚
